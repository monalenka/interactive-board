# Интерактивная Доска

Интерактивная доска с возможностью совместной работы в реальном времени.

## Возможности

- Рисование карандашом с настройкой цвета, размера и прозрачности
- Инструмент выделения объектов
- Добавление текста
- Ластик с настраиваемым размером
- Инструмент "Рука" для перемещения по доске
- Масштабирование колесиком мыши
- Синхронизация в реальном времени между устройствами
- Поддержка комнат для групповой работы

## Установка и запуск

### Предварительные требования
- Node.js (версия 16 или выше)
- npm

### Быстрый запуск (рекомендуется)

1. Установите все зависимости:
```bash
npm run install-all
```

2. Запустите сервер и клиент одновременно:
```bash
npm run dev
```

### Запуск по отдельности

#### Сервер

1. Перейдите в папку сервера:
```bash
cd server
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите сервер:
```bash
npm run dev
```

Сервер будет доступен по адресу: http://localhost:3001

#### Клиент

1. В новом терминале перейдите в папку клиента:
```bash
cd client
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите клиент:
```bash
npm run dev
```

Клиент будет доступен по адресу: http://localhost:5173

## Использование

1. Откройте браузер и перейдите по адресу http://localhost:5173
2. Интерактивная доска автоматически подключится к серверу
3. Для совместной работы:
   - Откройте ту же страницу на другом устройстве или в другой вкладке
   - Или используйте разные комнаты для разделения групп
4. Все изменения будут синхронизироваться в реальном времени

### Комнаты

- По умолчанию все пользователи подключаются к комнате "room1"
- Для создания отдельной сессии введите новый ID комнаты в селекторе комнат
- Пользователи в разных комнатах не видят изменения друг друга
- Это позволяет организовать несколько независимых сессий

### Инструменты

- **✏️ Карандаш** - рисование с настраиваемыми параметрами (цвет, размер, прозрачность)
- **➟ Указатель** - выделение и перемещение объектов
- **Текст** - добавление текстовых элементов (кликните на доску для создания)
- **🧽 Ластик** - удаление объектов (настраиваемый размер)
- **✋ Рука** - перемещение по доске (удерживайте и перетаскивайте)

### Горячие клавиши

- **Delete** - удаление выбранного объекта
- **Колесико мыши** - масштабирование

## Технологии

- **Frontend**: React, TypeScript, Fabric.js
- **Backend**: Node.js, Express, Socket.IO
- **Синхронизация**: WebSocket через Socket.IO 